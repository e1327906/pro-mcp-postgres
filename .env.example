# Multi-Database PostgreSQL MCP Server Configuration
# Copy this file to .env and update with your actual database credentials

# Method 1: Multiple databases in POSTGRES_CONNECTION_STRING (comma-separated)
# First database becomes "primary", subsequent ones become "db_2", "db_3", etc.
POSTGRES_CONNECTION_STRING=postgresql://user1:pass1@host1:5432/db1,postgresql://user2:pass2@host2:5432/db2

# Method 2: Individual named database connections
# Use POSTGRES_DB_<NAME> format for named connections
POSTGRES_DB_PRODUCTION=postgresql://prod_user:prod_pass@prod-host:5432/prod_db
POSTGRES_DB_STAGING=postgresql://staging_user:staging_pass@staging-host:5432/staging_db
POSTGRES_DB_DEVELOPMENT=postgresql://dev_user:dev_pass@localhost:5432/dev_db
POSTGRES_DB_ANALYTICS=postgresql://analytics_user:analytics_pass@analytics-host:5432/analytics_db

# Method 3: Single database (original format still supported)
# POSTGRES_CONNECTION_STRING=postgresql://username:password@localhost:5432/mydatabase

# Example with SSL:
# POSTGRES_CONNECTION_STRING=postgresql://username:password@host:5432/database?ssl=true

# You can combine both approaches - comma-separated and named connections work together
# Example for local development:
# POSTGRES_CONNECTION_STRING=postgresql://postgres:password@localhost:5432/testdb
